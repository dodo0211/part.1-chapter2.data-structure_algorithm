
    // for(int i=0; i<strlen(b) -1; i++) { // 마지막은 항상 공백이 들어가므로 -1 (line 55참고)
    //     if(b[i] == ' ') size++;
    // }
    // char *ptr2 = strtok(b, " ");
    // for(int i=0; i<size; i++) {
    //     strcpy(input[i], ptr2);
    //     ptr2 = strtok(NULL, " ");
    // }
    // calculate(&stack, input, size);